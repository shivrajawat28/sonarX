# Test fixtures

`fixture_sonar.png` — SYNTHETIC sonar-like pattern generated deterministically
by `ml/tests/fixtures.py::make_sonar_like_image(seed=7, width=256, height=128)`.
It is a software-test fixture only: it does NOT represent real seafloor sonar
data and must never be used to claim detection performance.
