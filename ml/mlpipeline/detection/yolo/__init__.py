"""YOLO adapter (Section 8.2). Import registers the 'yolo' detector kind.

Requires the optional `torch`/`ultralytics` extras; importing this module does
NOT require them — only instantiating/loading a model does (OPEN decision #5).
"""
