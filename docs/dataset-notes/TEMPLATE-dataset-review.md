# Dataset Review: <name>

> One document per candidate dataset. The final class list (Appendix A, OPEN #1) can only be closed after these reviews. **Do not delete even negative results** — they protect the team from re-evaluating dead ends.

- **Reviewed by / date:** —
- **Source / URL / license:** —
- **Access status:** public download / request required / unavailable
- **Modality:** side-scan sonar / synthetic aperture sonar / optical / other — **must be sonar to count for training**
- **Imaging geometry:** waterfall (raw ping stream) / mosaic / tile crops; resolution; slant-range vs ground-range
- **Format:** images + sidecar labels (COCO/VOC/CSV)? proprietary binary? navigation embedded?
- **Navigation metadata:** present? format? coordinates trustworthy?
- **Label classes present (verbatim from data):** —
- **Approx instance counts per class:** —
- **Annotation quality spot-check:** sampled N images; plausible boxes? consistent class boundaries?
- **Overlap with our target classes (shipwreck/pipe/net/anomaly — provisional, config-only):** —
- **License compatible with SIH demo use?** —
- **Verdict:** candidate / fallback / reject — why
- **Next action:** converter needed (which format), inspection script findings link
